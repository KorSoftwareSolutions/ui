{
  "name": "universal-ui-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter example dev",
    "start": "pnpm --filter example start",
    "ts-check": "pnpm -r ts-check",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
