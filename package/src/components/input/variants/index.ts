import { useInputVariantDefault } from "./default";
import { useInputVariantSecondary } from "./secondary";

export const InputVariants = {
  default: useInputVariantDefault,
  secondary: useInputVariantSecondary,
};
